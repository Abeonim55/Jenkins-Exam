apiVersion: v1
kind: Secret
metadata:
  name: movie-db-secret
type: Opaque
stringData:
  POSTGRES_DB: moviedb
  POSTGRES_USER: movie
  POSTGRES_PASSWORD: moviepass
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-db
spec:
  replicas: 1
  selector:
    matchLabels: { app: movie-db }
  template:
    metadata:
      labels: { app: movie-db }
    spec:
      containers:
      - name: postgres
        image: postgres:12.1-alpine
        ports:
        - containerPort: 5432
        envFrom:
        - secretRef:
            name: movie-db-secret
---
apiVersion: v1
kind: Service
metadata:
  name: movie-db
spec:
  selector: { app: movie-db }
  ports:
  - name: pg
    port: 5432
    targetPort: 5432
